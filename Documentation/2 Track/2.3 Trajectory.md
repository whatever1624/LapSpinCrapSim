Since the track coordinate parameterisation thing is the minimum curvature trajectory, we need to store information separately for:
- Centreline
- Track limits (2 soft limits + 2 hard limits)

Trajectory class must have:
- Positional spline (mapping longitudinal track coordinate to the lateral track coordinate)
- Function to derive the trajectory angle relative to the x-axis

==Can the trajectory just exist as a spline?== i feel like it can just be a few helper functions along with the spline util stuff